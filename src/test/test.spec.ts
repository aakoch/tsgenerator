import t from 'tap'
// import { CliTransformer } from '../cliTransformer.js'
// import { Generator } from '../index.ts'
//
// t.test('returns Hello world', t => {
//   // t.pass('this passes')
//   // t.fail('this fails')
//   // t.ok(myThing, 'this passes if truthy')
//   const g = new Generator();
//   t.equal(g.fromObject({}), 'Hello world!')
//   // t.match( myThing, {
//   //   property: String,
//   // }, 'this passes if myThing.property is a string')
//   // // call this when you're done
//   t.end()
// })
//
//
//
//
// // t.test('async tests work like you would expect', async t => {
// //   t.equal(await myThing.Transform, 'whatever')
// //   // don't have to call t.end(), it'll just end when the
// //   // async stuff is all resolved.
// // })

t.test('testing whether tap can be loaded', t => {
  t.end();
})